

class Subject {
  bool tag = false;
  var id;
  var avatarUrl;
  var nickName;
  var desc;
  var dateTimeStr;
  var up;
  var content;

  Subject(this.tag, this.id, this.avatarUrl, this.nickName, this.desc,
      this.dateTimeStr, this.up, this.content);
}